[pytest]
markers =
    integration: mark integration tests
    unit: mark unit tests
    smoke: mark smoke tests
    ai: mark tests that call live AI backends (Cursor/Claude/Codex)
    comprehensive: exhaustive live permutations (MySQL, saved conns, cloud)
    slow: tests that may take >30s (cloud metrics, full data compare)

# addopts = -ra
